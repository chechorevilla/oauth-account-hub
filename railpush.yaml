services:
  - name: oauth-hub
    type: web
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT"
    port: 5050
    autoDeploy: true
    plan: free
